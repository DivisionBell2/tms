# Порт мониторинга (http://localhost:8222)
http_port: 8222

# Максимальный размер сообщения. Файлы передаются через NATS в виде base64,
# поэтому лимит по умолчанию (1MB) слишком мал. 8MB хватает на файлы до ~5MB.
max_payload: 8MB

jetstream {
  store_dir: "/data"
}
